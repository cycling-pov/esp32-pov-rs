[package]
name = "pov-images"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25", features = ["jpeg", "gif"] }
pov-algs = { version = "*", path = "../pov-algs" }

[features]
default = []
all_formats = ["image/default-formats"]
