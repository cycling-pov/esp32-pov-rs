# pov-sender-core

Shared host-side sender logic for POV bridge tools.

This crate contains reusable serial/protocol/image-transfer logic used by:

- `tools/pov-sender-cli`
- `tools/pov-sender-gui`

## Build

```sh
cd tools/pov-sender-core
cargo build
```
